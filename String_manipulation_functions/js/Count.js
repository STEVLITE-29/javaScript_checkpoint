// function to count the total number of characters in a given string
function countChar(str) {
  // directly setting the of count to be the length of the string, which counts all characters (including spaces)
  let count = str.length;
  // returning the value of count
  return count;
}

// a use case sample
console.log(countChar("Nigeria is a country in Africa")) 